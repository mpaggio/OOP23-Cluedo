package it.unibo.cluedo.controller.dicecontroller.api;

/**
 * Interface for the controller class that handles the dice logic.
 */
public interface DiceController {

    /**
     * Paints the result of the dice roll.
     */
    void paintdICE(int result);
}
